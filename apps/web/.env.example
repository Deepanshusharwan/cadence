NEXT_PUBLIC_API_URL=http://localhost:8000

# Clerk (not wired up yet) — see backend/.env.example for the matching
# backend-side keys once a real Clerk project exists.
# NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
# CLERK_SECRET_KEY=
